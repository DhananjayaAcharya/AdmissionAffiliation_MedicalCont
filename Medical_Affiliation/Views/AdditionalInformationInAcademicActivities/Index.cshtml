
@model Medical_Affiliation.Models.AdditionalInformationInAcademicActivitiesVM

@{
    ViewData["Title"] = "Additional Information";
    Layout = "~/Views/Shared/_CaLayout.cshtml";
}
<style>
    /* ==========================================================
       ADDITIONAL INFORMATION IN ACADEMIC ACTIVITIES
       PAGE-SPECIFIC CSS
       ========================================================== */
    .aiaa-wrapper {
        width: 100%;
        max-width: 1450px;
        margin: 0 auto;
        padding: 10px 0 35px;
    }


    /* ==========================================================
       HEADER
       ========================================================== */

    .aiaa-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 18px 20px;
        margin-bottom: 16px;
        background: #ffffff;
        border: 1px solid #e1e6ec;
        border-radius: 10px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
    }

    .aiaa-header-left {
        display: flex;
        align-items: center;
        gap: 13px;
    }

    .aiaa-header-icon {
        width: 46px;
        height: 46px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: #edf4ff;
        color: #1a4c8b;
        border: 1px solid #d8e5f5;
        border-radius: 9px;
        font-size: 20px;
    }

    .aiaa-header h2 {
        margin: 0;
        color: #26384f;
        font-size: 20px;
        font-weight: 700;
    }

    .aiaa-header p {
        margin: 4px 0 0;
        color: #7d8793;
        font-size: 12px;
    }

    .aiaa-course-badge {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        padding: 7px 13px;
        background: #f5f8fc;
        border: 1px solid #dbe3ec;
        border-radius: 20px;
        color: #1a4c8b;
        font-size: 12px;
        font-weight: 700;
    }


    /* ==========================================================
       CARD
       ========================================================== */

    .aiaa-card {
        margin-bottom: 15px;
        background: #ffffff;
        border: 1px solid #e1e6ec;
        border-radius: 10px;
        overflow: hidden;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.035);
    }


    /* ==========================================================
       CARD HEADER
       ========================================================== */

    .aiaa-card-header {
        display: flex;
        align-items: center;
        gap: 12px;
        padding: 13px 16px;
        background: #f8fafc;
        border-bottom: 1px solid #e6ebf0;
    }

    .aiaa-number {
        width: 35px;
        height: 35px;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
        background: #eaf2ff;
        color: #1a4c8b;
        border-radius: 7px;
        font-size: 11px;
        font-weight: 800;
    }

    .aiaa-card-heading {
        flex: 1;
    }

        .aiaa-card-heading h3 {
            margin: 0;
            color: #344256;
            font-size: 14px;
            font-weight: 700;
        }

        .aiaa-card-heading p {
            margin: 3px 0 0;
            color: #858e99;
            font-size: 11px;
        }

    .aiaa-card-icon {
        color: #1a4c8b;
        font-size: 19px;
    }


    /* ==========================================================
       CARD BODY
       ========================================================== */

    .aiaa-card-body {
        padding: 16px;
    }


    /* ==========================================================
       QUESTION
       ========================================================== */

    .aiaa-question-box {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 25px;
        padding: 14px;
        background: #fbfcfe;
        border: 1px solid #e3e8ee;
        border-radius: 8px;
    }

    .aiaa-question {
        display: flex;
        align-items: center;
        gap: 10px;
    }

    .aiaa-question-icon {
        width: 38px;
        height: 38px;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
        background: #edf4ff;
        color: #1a4c8b;
        border-radius: 7px;
    }

    .aiaa-question h4 {
        margin: 0;
        color: #3d4958;
        font-size: 13px;
        font-weight: 600;
    }

    .aiaa-question span {
        display: block;
        margin-top: 3px;
        color: #8b949e;
        font-size: 11px;
    }


    /* ==========================================================
       YES / NO OPTIONS
       ========================================================== */

    .aiaa-options {
        display: flex;
        gap: 8px;
    }

    .aiaa-option {
        position: relative;
        margin: 0;
        cursor: pointer;
    }

        .aiaa-option input {
            position: absolute;
            opacity: 0;
        }

    .aiaa-option-content {
        display: flex;
        align-items: center;
        gap: 7px;
        min-width: 105px;
        padding: 7px 10px;
        background: #ffffff;
        border: 1px solid #d9e0e8;
        border-radius: 7px;
        transition: 0.2s ease;
    }

    .aiaa-option input:checked +
    .aiaa-option-content {
        border-color: #1a4c8b;
        background: #f3f7fd;
        box-shadow: 0 0 0 2px rgba(26, 76, 139, 0.06);
    }

    .aiaa-option-icon {
        width: 27px;
        height: 27px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 6px;
        font-size: 12px;
    }

    .aiaa-success {
        background: #eaf8f0;
        color: #198754;
    }

    .aiaa-danger {
        background: #fff0f0;
        color: #dc3545;
    }

    .aiaa-option strong {
        display: block;
        color: #3d4857;
        font-size: 11px;
    }

    .aiaa-option small {
        display: block;
        color: #8b949e;
        font-size: 9px;
    }


    /* ==========================================================
       NUMBER INPUT
       ========================================================== */

    .aiaa-input-row {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 30px;
        padding: 14px;
        background: #fbfcfe;
        border: 1px solid #e3e8ee;
        border-radius: 8px;
    }

    .aiaa-input-information {
        display: flex;
        align-items: center;
        gap: 10px;
    }

    .aiaa-input-icon {
        width: 38px;
        height: 38px;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
        background: #edf4ff;
        color: #1a4c8b;
        border-radius: 7px;
    }

    .aiaa-input-information h4 {
        margin: 0;
        color: #3d4958;
        font-size: 13px;
        font-weight: 600;
    }

    .aiaa-input-information p {
        margin: 3px 0 0;
        color: #8b949e;
        font-size: 11px;
    }

    .aiaa-input-group {
        width: 220px;
    }

        .aiaa-input-group label {
            display: block;
            margin-bottom: 5px;
            color: #4b5665;
            font-size: 11px;
            font-weight: 600;
        }

            .aiaa-input-group label span {
                color: #dc3545;
            }

    .aiaa-number-input {
        width: 100%;
        height: 38px;
        padding: 7px 10px;
        background: #ffffff;
        border: 1px solid #d5dde6;
        border-radius: 6px;
        color: #344054;
        font-size: 12px;
        outline: none;
    }

        .aiaa-number-input:focus {
            border-color: #1a4c8b;
            box-shadow: 0 0 0 3px rgba(26, 76, 139, 0.07);
        }

        .aiaa-number-input::placeholder {
            color: #a7afb9;
        }

    .aiaa-error {
        display: block;
        margin-top: 4px;
        color: #dc3545;
        font-size: 10px;
    }


    /* ==========================================================
       CME UPLOAD
       ========================================================== */

    .aiaa-upload-box {
        display: grid;
        grid-template-columns: 1fr 1.4fr;
        gap: 18px;
        padding: 15px;
        background: #fbfcfe;
        border: 1px solid #e3e8ee;
        border-radius: 8px;
    }

    .aiaa-upload-info {
        display: flex;
        align-items: flex-start;
        gap: 11px;
    }

    .aiaa-pdf-icon {
        width: 42px;
        height: 42px;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
        background: #fff0f0;
        color: #dc3545;
        border-radius: 8px;
        font-size: 18px;
    }

    .aiaa-upload-info h4 {
        margin: 2px 0 0;
        color: #3d4958;
        font-size: 13px;
        font-weight: 600;
    }

    .aiaa-upload-info p {
        margin: 4px 0 8px;
        color: #858e99;
        font-size: 11px;
    }

    .aiaa-file-info {
        display: flex;
        gap: 12px;
    }

        .aiaa-file-info span {
            display: inline-flex;
            align-items: center;
            gap: 4px;
            color: #737d89;
            font-size: 10px;
        }

        .aiaa-file-info i {
            color: #1a4c8b;
        }


    /* ==========================================================
       FILE INPUT
       ========================================================== */

    .aiaa-upload-right {
        display: flex;
        align-items: center;
        justify-content: center;
        flex-wrap: wrap;
        gap: 9px;
        padding: 12px;
        background: #ffffff;
        border: 1px dashed #cbd5e1;
        border-radius: 7px;
    }

    .aiaa-file-input {
        display: none;
    }

    .aiaa-upload-button {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 6px;
        height: 38px;
        padding: 7px 14px;
        background: #1a4c8b;
        color: #ffffff;
        border-radius: 6px;
        font-size: 11px;
        font-weight: 600;
        cursor: pointer;
        transition: 0.2s ease;
    }

        .aiaa-upload-button:hover {
            background: #143d70;
        }


    /* ==========================================================
       SELECTED FILE
       ========================================================== */

    .aiaa-selected-file {
        width: 100%;
        display: none;
        align-items: center;
        justify-content: space-between;
        padding: 8px 10px;
        background: #f7fafc;
        border: 1px solid #dce3eb;
        border-radius: 6px;
    }

    .aiaa-selected-file-left {
        display: flex;
        align-items: center;
        gap: 8px;
        min-width: 0;
    }

        .aiaa-selected-file-left > i {
            color: #dc3545;
            font-size: 20px;
        }

        .aiaa-selected-file-left strong {
            display: block;
            max-width: 350px;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
            color: #3e4958;
            font-size: 11px;
        }

        .aiaa-selected-file-left small {
            display: block;
            margin-top: 2px;
            color: #929aa4;
            font-size: 9px;
        }

    .aiaa-remove-button {
        width: 27px;
        height: 27px;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 0;
        background: #fff0f0;
        border: none;
        border-radius: 5px;
        color: #dc3545;
        cursor: pointer;
    }

        .aiaa-remove-button:hover {
            background: #dc3545;
            color: #ffffff;
        }


    /* ==========================================================
       EXISTING FILE
       ========================================================== */

    .aiaa-existing-file {
        width: 100%;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 10px;
        padding: 8px 10px;
        background: #f8fafc;
        border: 1px solid #dce3eb;
        border-radius: 6px;
    }

    .aiaa-existing-left {
        display: flex;
        align-items: center;
        gap: 8px;
    }

    .aiaa-existing-icon {
        width: 30px;
        height: 30px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: #fff0f0;
        color: #dc3545;
        border-radius: 5px;
        font-size: 15px;
    }

    .aiaa-existing-left strong {
        display: block;
        color: #3e4958;
        font-size: 10px;
    }

    .aiaa-existing-left small {
        display: block;
        margin-top: 2px;
        color: #929aa4;
        font-size: 9px;
    }

    .aiaa-view-button {
        display: inline-flex;
        align-items: center;
        gap: 5px;
        padding: 6px 10px;
        background: #ffffff;
        border: 1px solid #1a4c8b;
        border-radius: 5px;
        color: #1a4c8b;
        font-size: 10px;
        font-weight: 600;
        text-decoration: none;
    }

        .aiaa-view-button:hover {
            background: #1a4c8b;
            color: #ffffff;
        }


    /* ==========================================================
       FOOTER
       ========================================================== */

    .aiaa-footer {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 15px;
        padding: 3px 1px 0;
    }

    .aiaa-footer-note {
        display: flex;
        align-items: center;
        gap: 6px;
        color: #7c8793;
        font-size: 10px;
    }

        .aiaa-footer-note i {
            color: #1a4c8b;
        }

    .aiaa-save-button {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 6px;
        min-width: 155px;
        height: 39px;
        padding: 7px 15px;
        background: #1a4c8b;
        border: none;
        border-radius: 6px;
        color: #ffffff;
        font-size: 11px;
        font-weight: 600;
        cursor: pointer;
        box-shadow: 0 3px 8px rgba(26, 76, 139, 0.15);
        transition: 0.2s ease;
    }

        .aiaa-save-button:hover {
            background: #143d70;
            transform: translateY(-1px);
        }


    /* ==========================================================
       RESPONSIVE
       ========================================================== */

    @@media (max-width: 900px) {

        .aiaa-upload-box {
            grid-template-columns: 1fr;
        }

        .aiaa-question-box,
        .aiaa-input-row {
            align-items: flex-start;
            flex-direction: column;
        }

        .aiaa-input-group {
            width: 100%;
            max-width: 280px;
        }
    }


    @@media (max-width: 600px) {

        .aiaa-header {
            align-items: flex-start;
            flex-direction: column;
        }

        .aiaa-course-badge {
            align-self: flex-start;
        }

        .aiaa-question-box {
            padding: 12px;
        }

        .aiaa-options {
            width: 100%;
        }

        .aiaa-option {
            flex: 1;
        }

        .aiaa-option-content {
            justify-content: center;
            min-width: 0;
        }

        .aiaa-input-group {
            width: 100%;
            max-width: none;
        }

        .aiaa-upload-right {
            flex-direction: column;
        }

        .aiaa-upload-button {
            width: 100%;
        }

        .aiaa-existing-file {
            align-items: flex-start;
            flex-direction: column;
        }

        .aiaa-view-button {
            width: 100%;
            justify-content: center;
        }

        .aiaa-footer {
            align-items: stretch;
            flex-direction: column;
        }

        .aiaa-save-button {
            width: 100%;
        }
    }
</style>
<style>
    /* ==========================================================
       CONFERENCES CONDUCTED
       ========================================================== */

    .conference-toolbar {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 20px;
        margin-bottom: 15px;
        padding: 14px 16px;
        background: #fbfcfe;
        border: 1px solid #e3e8ee;
        border-radius: 8px;
    }

    .conference-toolbar h4 {
        margin: 0;
        color: #344256;
        font-size: 14px;
        font-weight: 700;
    }

    .conference-toolbar p {
        margin: 4px 0 0;
        color: #858e99;
        font-size: 11px;
    }


    /* ==========================================================
       ADD CONFERENCE BUTTON
       ========================================================== */

    .aiaa-add-conference-button {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 7px;
        min-width: 145px;
        height: 37px;
        padding: 7px 14px;
        background: #1a4c8b;
        border: none;
        border-radius: 6px;
        color: #ffffff;
        font-size: 11px;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.2s ease;
        box-shadow: 0 3px 8px rgba(26, 76, 139, 0.12);
    }

    .aiaa-add-conference-button:hover {
        background: #143d70;
        transform: translateY(-1px);
        box-shadow: 0 4px 10px rgba(26, 76, 139, 0.18);
    }

    .aiaa-add-conference-button i {
        font-size: 13px;
    }


    /* ==========================================================
       CONFERENCE CONTAINER
       ========================================================== */

    #conferenceContainer {
        display: flex;
        flex-direction: column;
        gap: 12px;
    }


    /* ==========================================================
       CONFERENCE ITEM
       ========================================================== */

    .conference-item {
        background: #ffffff;
        border: 1px solid #dfe5ec;
        border-radius: 8px;
        overflow: hidden;
        box-shadow: 0 2px 6px rgba(0, 0, 0, 0.025);
        transition: border-color 0.2s ease,
                    box-shadow 0.2s ease;
    }

    .conference-item:hover {
        border-color: #cbd7e5;
        box-shadow: 0 3px 9px rgba(0, 0, 0, 0.045);
    }


    /* ==========================================================
       CONFERENCE HEADER
       ========================================================== */

    .conference-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 10px 13px;
        background: #f8fafc;
        border-bottom: 1px solid #e6ebf0;
    }

    .conference-title {
        display: flex;
        align-items: center;
        gap: 9px;
    }

    .conference-title strong {
        color: #344256;
        font-size: 12px;
        font-weight: 700;
    }


    /* ==========================================================
       CONFERENCE NUMBER
       ========================================================== */

    .conference-number {
        width: 31px;
        height: 31px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        background: #eaf2ff;
        color: #1a4c8b;
        border-radius: 6px;
        font-size: 10px;
        font-weight: 800;
    }


    /* ==========================================================
       REMOVE BUTTON
       ========================================================== */

    .conference-remove-button {
        width: 30px;
        height: 30px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 0;
        background: #fff0f0;
        border: 1px solid #f4d2d2;
        border-radius: 6px;
        color: #dc3545;
        cursor: pointer;
        transition: all 0.2s ease;
    }

    .conference-remove-button:hover {
        background: #dc3545;
        border-color: #dc3545;
        color: #ffffff;
    }

    .conference-remove-button i {
        font-size: 12px;
    }


    /* ==========================================================
       CONFERENCE BODY
       ========================================================== */

    .conference-body {
        display: grid;
        grid-template-columns: 1.5fr 1.2fr 0.8fr;
        gap: 15px;
        padding: 15px;
        background: #ffffff;
    }


    /* ==========================================================
       FORM GROUP
       ========================================================== */

    .conference-body .form-group {
        min-width: 0;
    }

    .conference-body .form-group label {
        display: block;
        margin-bottom: 5px;
        color: #4b5665;
        font-size: 11px;
        font-weight: 600;
    }

    .conference-body .form-group label::after {
        content: " *";
        color: #dc3545;
    }


    /* ==========================================================
       INPUTS
       ========================================================== */

    .conference-body .form-control {
        width: 100%;
        height: 38px;
        padding: 7px 10px;
        background: #ffffff;
        border: 1px solid #d5dde6;
        border-radius: 6px;
        color: #344054;
        font-size: 12px;
        outline: none;
        transition: all 0.2s ease;
    }

    .conference-body .form-control:focus {
        border-color: #1a4c8b;
        box-shadow: 0 0 0 3px rgba(26, 76, 139, 0.07);
    }

    .conference-body .form-control::placeholder {
        color: #a7afb9;
    }


    /* ==========================================================
       DATE INPUT
       ========================================================== */

    .conference-body input[type="date"] {
        cursor: pointer;
    }

    .conference-body input[type="date"]::-webkit-calendar-picker-indicator {
        cursor: pointer;
        opacity: 0.7;
    }


    /* ==========================================================
       EMPTY STATE
       ========================================================== */

    .conference-empty-state {
        display: block;
        margin-top: 12px;
        padding: 28px 20px;
        text-align: center;
        background: #fbfcfe;
        border: 1px dashed #cbd5e1;
        border-radius: 8px;
        color: #8b949e;
    }

    .conference-empty-state i {
        display: block;
        margin-bottom: 7px;
        color: #9aa6b2;
        font-size: 27px;
    }

    .conference-empty-state p {
        margin: 0;
        color: #6f7b88;
        font-size: 12px;
        font-weight: 600;
    }

    .conference-empty-state small {
        display: block;
        margin-top: 4px;
        color: #9aa3ad;
        font-size: 10px;
    }


    /* ==========================================================
       RESPONSIVE - TABLET
       ========================================================== */

    @@media (max-width: 1000px) {

        .conference-body {
            grid-template-columns: 1fr 1fr;
        }

        .conference-body .form-group:last-child {
            grid-column: span 2;
        }
    }


    /* ==========================================================
       RESPONSIVE - MOBILE
       ========================================================== */

    @@media (max-width: 650px) {

        .conference-toolbar {
            align-items: stretch;
            flex-direction: column;
        }

        .aiaa-add-conference-button {
            width: 100%;
        }

        .conference-body {
            grid-template-columns: 1fr;
            gap: 12px;
        }

        .conference-body .form-group:last-child {
            grid-column: auto;
        }

        .conference-header {
            padding: 9px 10px;
        }

        .conference-body {
            padding: 12px;
        }

        .conference-remove-button {
            width: 28px;
            height: 28px;
        }
    }
</style>
<div class="aiaa-wrapper">

    <!-- =====================================================
         PAGE HEADER
         ===================================================== -->

    <div class="aiaa-header">

        <div class="aiaa-header-left">

            <div class="aiaa-header-icon">
                <i class="bi bi-mortarboard-fill"></i>
            </div>

            <div>
                <h2>Additional Information in Academic Activities</h2>

                <p>
                    Academic activities, Medical Education Unit,
                    TOT programmes and CME details
                </p>
            </div>

        </div>

        <div class="aiaa-course-badge">
            <i class="bi bi-bookmark"></i>
            @Model.CourseLevel
        </div>

    </div>


    <form asp-action="Save"
          method="post"
          enctype="multipart/form-data"
          id="aiaaForm">

        @Html.AntiForgeryToken()

        <input type="hidden" asp-for="Id" />
        <input type="hidden" asp-for="FacultyCode" />
        <input type="hidden" asp-for="CollegeCode" />
        <input type="hidden" asp-for="CourseLevel" />
        <input type="hidden" asp-for="CMEProgrammePdfPath" />


        <!-- =====================================================
             01. MEDICAL EDUCATION UNIT
             ===================================================== -->

        <div class="aiaa-card">

            <div class="aiaa-card-header">

                <div class="aiaa-number">
                    01
                </div>

                <div class="aiaa-card-heading">

                    <h3>Medical Education Unit</h3>

                    <p>
                        Information regarding the Medical Education
                        Unit available in the institution.
                    </p>

                </div>

                <i class="bi bi-building aiaa-card-icon"></i>

            </div>


            <div class="aiaa-card-body">

                <div class="aiaa-question-box">

                    <div class="aiaa-question">

                        <div class="aiaa-question-icon">
                            <i class="bi bi-hospital"></i>
                        </div>

                        <div>

                            <h4>
                                Does the institution have a
                                Medical Education Unit?
                            </h4>

                            <span>
                                Select Yes or No.
                            </span>

                        </div>

                    </div>


                    <div class="aiaa-options">

                        <label class="aiaa-option">

                            <input type="radio"
                                   asp-for="HasMedicalEducationUnit"
                                   value="true" />

                            <span class="aiaa-option-content">

                                <span class="aiaa-option-icon aiaa-success">
                                    <i class="bi bi-check-lg"></i>
                                </span>

                                <span>
                                    <strong>Yes</strong>
                                    <small>Available</small>
                                </span>

                            </span>

                        </label>


                        <label class="aiaa-option">

                            <input type="radio"
                                   asp-for="HasMedicalEducationUnit"
                                   value="false" />

                            <span class="aiaa-option-content">

                                <span class="aiaa-option-icon aiaa-danger">
                                    <i class="bi bi-x-lg"></i>
                                </span>

                                <span>
                                    <strong>No</strong>
                                    <small>Not Available</small>
                                </span>

                            </span>

                        </label>

                    </div>

                </div>

            </div>

        </div>


        <!-- =====================================================
             02. TOT PROGRAMMES CONDUCTED
             ===================================================== -->

        <!-- TOT PROGRAMMES CONDUCTED -->

        <div class="aiaa-question-box">

            <div class="aiaa-question">
                <div class="aiaa-question-icon">
                    <i class="bi bi-calendar-check"></i>
                </div>

                <div>
                    <h4>Has the institution conducted TOT programmes?</h4>
                    <span>Select Yes or No.</span>
                </div>
            </div>

            <div class="aiaa-options">

                <label class="aiaa-option">
                    <input type="radio"
                           asp-for="HasTOTProgrammesConducted"
                           value="true"
                           onchange="toggleConductedCount()" />

                    <span class="aiaa-option-content">
                        <span class="aiaa-option-icon aiaa-success">
                            <i class="bi bi-check-lg"></i>
                        </span>

                        <span>
                            <strong>Yes</strong>
                            <small>Conducted</small>
                        </span>
                    </span>
                </label>


                <label class="aiaa-option">
                    <input type="radio"
                           asp-for="HasTOTProgrammesConducted"
                           value="false"
                           onchange="toggleConductedCount()" />

                    <span class="aiaa-option-content">
                        <span class="aiaa-option-icon aiaa-danger">
                            <i class="bi bi-x-lg"></i>
                        </span>

                        <span>
                            <strong>No</strong>
                            <small>Not Conducted</small>
                        </span>
                    </span>
                </label>

            </div>

        </div>


        <!-- SHOW ONLY WHEN YES -->

        <div id="conductedCountSection"
             class="aiaa-dependent-section"
             style="display:none;">

            <div class="aiaa-input-row">

                <div class="aiaa-input-information">

                    <div class="aiaa-input-icon">
                        <i class="bi bi-hash"></i>
                    </div>

                    <div>
                        <h4>Number of TOT programmes conducted</h4>
                        <p>Enter the total number of programmes conducted.</p>
                    </div>

                </div>

                <div class="aiaa-input-group">

                    <label asp-for="TOTProgrammesConducted">
                        No. of Programmes
                    </label>

                    <input asp-for="TOTProgrammesConducted"
                           type="number"
                           min="0"
                           class="aiaa-number-input"
                           placeholder="Enter number" />

                    <span asp-validation-for="TOTProgrammesConducted"
                          class="aiaa-error">
                    </span>

                </div>

            </div>

        </div>

        <!-- =====================================================
         03. TOT PROGRAMMES ATTENDED
         ===================================================== -->

        <div class="aiaa-card">

            <div class="aiaa-card-header">

                <div class="aiaa-number">
                    03
                </div>

                <div class="aiaa-card-heading">

                    <h3>TOT Programmes Attended</h3>

                    <p>
                        Training of Trainers programmes attended by the faculty.
                    </p>

                </div>

                <i class="bi bi-people aiaa-card-icon"></i>

            </div>


            <div class="aiaa-card-body">

                <!-- YES / NO -->

                <div class="aiaa-question-box">

                    <div class="aiaa-question">

                        <div class="aiaa-question-icon">
                            <i class="bi bi-person-check"></i>
                        </div>

                        <div>
                            <h4>
                                Has the faculty attended TOT programmes?
                            </h4>

                            <span>
                                Select Yes or No.
                            </span>
                        </div>

                    </div>


                    <div class="aiaa-options">

                        <!-- YES -->

                        <label class="aiaa-option">

                            <input type="radio"
                                   asp-for="HasTOTProgrammesAttended"
                                   value="true"
                                   onchange="toggleAttendedCount()" />

                            <span class="aiaa-option-content">

                                <span class="aiaa-option-icon aiaa-success">
                                    <i class="bi bi-check-lg"></i>
                                </span>

                                <span>
                                    <strong>Yes</strong>
                                    <small>Attended</small>
                                </span>

                            </span>

                        </label>


                        <!-- NO -->

                        <label class="aiaa-option">

                            <input type="radio"
                                   asp-for="HasTOTProgrammesAttended"
                                   value="false"
                                   onchange="toggleAttendedCount()" />

                            <span class="aiaa-option-content">

                                <span class="aiaa-option-icon aiaa-danger">
                                    <i class="bi bi-x-lg"></i>
                                </span>

                                <span>
                                    <strong>No</strong>
                                    <small>Not Attended</small>
                                </span>

                            </span>

                        </label>

                    </div>

                </div>


                <!-- =================================================
                 NUMBER OF PROGRAMMES
                 SHOWN ONLY WHEN YES IS SELECTED
             ================================================= -->

                <div id="attendedCountSection"
                     class="aiaa-dependent-section"
                     style="display:none;">

                    <div class="aiaa-input-row">

                        <div class="aiaa-input-information">

                            <div class="aiaa-input-icon">
                                <i class="bi bi-hash"></i>
                            </div>

                            <div>

                                <h4>
                                    Number of TOT programmes attended
                                </h4>

                                <p>
                                    Enter the total number of TOT programmes
                                    attended by the faculty.
                                </p>

                            </div>

                        </div>


                        <div class="aiaa-input-group">

                            <label asp-for="TOTProgrammesAttended">
                                No. of Programmes
                            </label>

                            <input asp-for="TOTProgrammesAttended"
                                   type="number"
                                   min="0"
                                   class="aiaa-number-input"
                                   placeholder="Enter number" />

                            <span asp-validation-for="TOTProgrammesAttended"
                                  class="aiaa-error">
                            </span>

                        </div>

                    </div>

                </div>

            </div>

        </div>

        <!-- SHOW ONLY WHEN YES -->

        @* <div id="attendedCountSection"
             class="aiaa-dependent-section"
             style="display:none;">

            <div class="aiaa-input-row">

                <div class="aiaa-input-information">

                    <div class="aiaa-input-icon">
                        <i class="bi bi-hash"></i>
                    </div>

                    <div>
                        <h4>Number of TOT programmes attended</h4>
                        <p>Enter the total number of programmes attended.</p>
                    </div>

                </div>

                <div class="aiaa-input-group">

                    <label asp-for="TOTProgrammesAttended">
                        No. of Programmes
                    </label>

                    <input asp-for="TOTProgrammesAttended"
                           type="number"
                           min="0"
                           class="aiaa-number-input"
                           placeholder="Enter number" />

                    <span asp-validation-for="TOTProgrammesAttended"
                          class="aiaa-error">
                    </span>

                </div>

            </div>

        </div> *@

        <!-- =====================================================
             04. CME PROGRAMME
             ===================================================== -->

        <div class="aiaa-card">

            <div class="aiaa-card-header">

                <div class="aiaa-number">
                    04
                </div>

                <div class="aiaa-card-heading">

                    <h3>CME Programme</h3>

                    <p>
                        Upload the supporting CME programme document.
                    </p>

                </div>

                <i class="bi bi-file-earmark-medical aiaa-card-icon"></i>

            </div>


            <div class="aiaa-card-body">

                <div class="aiaa-upload-box">

                    <div class="aiaa-upload-info">

                        <div class="aiaa-pdf-icon">
                            <i class="bi bi-file-earmark-pdf-fill"></i>
                        </div>

                        <div>

                            <h4>CME Programme PDF</h4>

                            <p>
                                Upload the CME programme document
                                in PDF format.
                            </p>

                            <div class="aiaa-file-info">

                                <span>
                                    <i class="bi bi-filetype-pdf"></i>
                                    PDF only
                                </span>

                                <span>
                                    <i class="bi bi-hdd"></i>
                                    Maximum 2 MB
                                </span>

                            </div>

                        </div>

                    </div>


                    <div class="aiaa-upload-right">

                        <label for="CMEProgrammePdf"
                               class="aiaa-upload-button">

                            <i class="bi bi-cloud-arrow-up"></i>

                            Choose PDF

                        </label>

                        <input asp-for="CMEProgrammePdf"
                               type="file"
                               id="CMEProgrammePdf"
                               accept="application/pdf"
                               class="aiaa-file-input"
                               onchange="handleCMEFileChange(this)" />


                        <div id="cmeFileDisplay"
                             class="aiaa-selected-file">

                            <div class="aiaa-selected-file-left">

                                <i class="bi bi-file-earmark-pdf-fill"></i>

                                <div>

                                    <strong class="name-text"></strong>

                                    <small>
                                        PDF selected
                                    </small>

                                </div>

                            </div>

                            <button type="button"
                                    class="aiaa-remove-button"
                                    onclick="removeCMEFile()">

                                <i class="bi bi-x-lg"></i>

                            </button>

                        </div>


                        @if (!string.IsNullOrWhiteSpace(Model.CMEProgrammePdfPath))
                        {
                            <div class="aiaa-existing-file">

                                <div class="aiaa-existing-left">

                                    <div class="aiaa-existing-icon">
                                        <i class="bi bi-file-earmark-pdf-fill"></i>
                                    </div>

                                    <div>

                                        <strong>
                                            Existing CME Programme PDF
                                        </strong>

                                        <small>
                                            Previously uploaded document
                                        </small>

                                    </div>

                                </div>


                                <a asp-action="ViewCMEProgrammePdf"
                                   asp-route-id="@Model.Id"
                                   target="_blank"
                                   class="aiaa-view-button">

                                    <i class="bi bi-eye"></i>

                                    View PDF

                                </a>

                            </div>
                        }

                    </div>

                </div>

            </div>

        </div>

        <!-- =====================================================
             05. CONFERENCES CONDUCTED
             ===================================================== -->

        <div class="aiaa-card">

            <div class="aiaa-card-header">

                <div class="aiaa-number">
                    05
                </div>

                <div class="aiaa-card-heading">

                    <h3>Conferences Conducted</h3>

                    <p>
                        Details of conferences conducted by the institution.
                    </p>

                </div>

                <i class="bi bi-calendar-event aiaa-card-icon"></i>

            </div>


            <div class="aiaa-card-body">

                <!-- ADD CONFERENCE BUTTON -->

                <div class="conference-toolbar">

                    <div>
                        <h4>Conference Details</h4>
                        <p>Add one or more conferences conducted.</p>
                    </div>

                    <button type="button"
                            class="aiaa-add-conference-button"
                            onclick="addConference()">

                        <i class="bi bi-plus-lg"></i>
                        Add Conference

                    </button>

                </div>


                <!-- =================================================
                     EXISTING + NEW CONFERENCES WILL COME HERE
                     ================================================= -->

                <div id="conferenceContainer">

                    @if (Model.Conferences != null && Model.Conferences.Any())
                    {
                        for (int i = 0; i < Model.Conferences.Count; i++)
                        {
                            <div class="conference-item">

                                <div class="conference-header">

                                    <div class="conference-title">

                                        <span class="conference-number">
                                            @((i + 1).ToString("D2"))
                                        </span>

                                        <strong>
                                            Conference @(i + 1)
                                        </strong>

                                    </div>

                                    <button type="button"
                                            class="conference-remove-button"
                                            onclick="removeConference(this)">

                                        <i class="bi bi-trash"></i>

                                    </button>

                                </div>


                                <div class="conference-body">

                                    <div class="form-group">

                                        <label>
                                            Conference Name
                                        </label>

                                        <input type="text"
                                               name="Conferences[@i].ConferenceName"
                                               value="@Model.Conferences[i].ConferenceName"
                                               class="form-control"
                                               placeholder="Enter conference name" />

                                    </div>


                                    <div class="form-group">

                                        <label>
                                            Conference Place
                                        </label>

                                        <input type="text"
                                               name="Conferences[@i].ConferencePlace"
                                               value="@Model.Conferences[i].ConferencePlace"
                                               class="form-control"
                                               placeholder="Enter conference place" />

                                    </div>


                                    <div class="form-group">

                                        <label>
                                            Conference Date
                                        </label>

                                        <input type="date"
                                               name="Conferences[@i].ConferenceDate"
                                               value="@(Model.Conferences[i].ConferenceDate?.ToString("yyyy-MM-dd"))"
                                               class="form-control" />

                                    </div>

                                </div>

                            </div>
                        }
                    }

                </div>


                <!-- EMPTY MESSAGE -->

                <div id="conferenceEmptyState"
                     class="conference-empty-state">

                    <i class="bi bi-calendar-x"></i>

                    <p>No conferences added yet.</p>

                    <small>
                        Click "Add Conference" to enter conference details.
                    </small>

                </div>


                <!-- =================================================
                     JAVASCRIPT TEMPLATE
                     ================================================= -->

                <template id="conferenceTemplate">

                    <div class="conference-item">

                        <div class="conference-header">

                            <div class="conference-title">

                                <span class="conference-number">
                                    01
                                </span>

                                <strong>
                                    Conference
                                </strong>

                            </div>


                            <button type="button"
                                    class="conference-remove-button"
                                    onclick="removeConference(this)">

                                <i class="bi bi-trash"></i>

                            </button>

                        </div>


                        <div class="conference-body">

                            <div class="form-group">

                                <label>
                                    Conference Name
                                </label>

                                <input type="text"
                                       name="Conferences[__INDEX__].ConferenceName"
                                       class="form-control"
                                       placeholder="Enter conference name" />

                            </div>


                            <div class="form-group">

                                <label>
                                    Conference Place
                                </label>

                                <input type="text"
                                       name="Conferences[__INDEX__].ConferencePlace"
                                       class="form-control"
                                       placeholder="Enter conference place" />

                            </div>


                            <div class="form-group">

                                <label>
                                    Conference Date
                                </label>

                                <input type="date"
                                       name="Conferences[__INDEX__].ConferenceDate"
                                       class="form-control" />

                            </div>

                        </div>

                    </div>

                </template>

            </div>

        </div>

        <!-- =====================================================
             SAVE
             ===================================================== -->

        <div class="aiaa-footer">

            <div class="aiaa-footer-note">

                <i class="bi bi-info-circle"></i>

                Please verify all information before saving.

            </div>


            <button type="submit"
                    class="aiaa-save-button">

                <i class="bi bi-check-circle"></i>

                Save Information

            </button>

        </div>

    </form>

</div>


<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

<script>

        // =========================================================
    // TOT PROGRAMMES CONDUCTED
    // =========================================================

    function toggleConductedCount() {

        const selected = document.querySelector(
            'input[name="HasTOTProgrammesConducted"]:checked'
        );

        const section = document.getElementById(
            "conductedCountSection"
        );

        const input = document.getElementById(
            "TotprogrammesConducted"
        );

        if (!selected) {
            section.style.display = "none";
            return;
        }

        if (selected.value === "true") {

            // YES → show number field
            section.style.display = "block";

        } else {

            // NO → hide and reset
            section.style.display = "none";

            if (input) {
                input.value = "0";
            }
        }
    }


    // =========================================================
    // TOT PROGRAMMES ATTENDED
    // =========================================================

    function toggleAttendedCount() {

        const selected = document.querySelector(
            'input[name="HasTOTProgrammesAttended"]:checked'
        );

        const section = document.getElementById(
            "attendedCountSection"
        );

        const input = document.getElementById(
            "TotprogrammesAttended"
        );

        if (!selected) {
            section.style.display = "none";
            return;
        }

        if (selected.value === "true") {

            // YES → SHOW number field
            section.style.display = "block";

        } else {

            // NO → HIDE number field
            section.style.display = "none";

            if (input) {
                input.value = "0";
            }
        }
    }


    // =========================================================
    // INITIALIZE WHEN PAGE LOADS
    // =========================================================

    document.addEventListener("DOMContentLoaded", function () {

        toggleConductedCount();
        toggleAttendedCount();

    });


    // ========================================================= 
    // CME FILE 
    // ========================================================= 
    function handleCMEFileChange(input) { 
        const file = input.files[0]; 
        const display = document.getElementById("cmeFileDisplay"); 
        if (!file) { 
            display.style.display = "none"; 
            return; 
        } 
        const isPdf = file.type === "application/pdf" || file.name.toLowerCase().endsWith(".pdf"); 
        if (!isPdf) { 
            Swal.fire({ 
                icon: "error", 
                title: "Invalid File Type", 
                text: "Only PDF files are allowed.", 
                confirmButtonColor: "#1a4c8b" 
            }); 
            input.value = ""; 
            display.style.display = "none"; 
            return; 
        } 
        const maxSize = 2 * 1024 * 1024; 
        if (file.size > maxSize) { 
            Swal.fire({ 
                icon: "error", 
                title: "File Too Large", 
                text: "File size must not exceed 2 MB.", 
                confirmButtonColor: "#1a4c8b" 
            }); input.value = ""; 
            display.style.display = "none"; 
            return; 
        } 
        display.querySelector(".name-text").textContent = file.name; 
        display.style.display = "flex"; 
    }

    @if (TempData["SuccessMessage"] != null)
    {
        <text>
            Swal.fire({
                icon: 'success',
                title: 'Success',
                text: '@Html.Raw(TempData["SuccessMessage"])',
                confirmButtonColor: '#1a4c8b',
                confirmButtonText: 'OK'
            });
        </text>
    }

    @if (TempData["ErrorMessage"] != null)
    {
        <text>
            Swal.fire({
                icon: 'error',
                title: 'Error',
                text: '@Html.Raw(TempData["ErrorMessage"])',
                confirmButtonColor: '#d33',
                confirmButtonText: 'OK'
            });
        </text>
    }


    // =========================================================
    // REMOVE CME FILE
    // =========================================================

    function removeCMEFile() {

        const input =
            document.getElementById("CMEProgrammePdf");

        const display =
            document.getElementById("cmeFileDisplay");

        input.value = "";

        display.style.display = "none";
    }


</script>

<script>

    // =========================================================
    // CONFERENCE INDEX
    // =========================================================

    let conferenceIndex = 0;


    // =========================================================
    // ADD CONFERENCE
    // =========================================================

    function addConference() {

        const container = document.getElementById("conferenceContainer");
        const template = document.getElementById("conferenceTemplate");

        if (!container) {
            console.error("conferenceContainer not found.");
            return;
        }

        if (!template) {
            console.error("conferenceTemplate not found.");
            return;
        }

        // Get template HTML
        let html = template.innerHTML;

        // Replace all __INDEX__ placeholders
        html = html.replace(/__INDEX__/g, conferenceIndex);

        // Add row/card
        container.insertAdjacentHTML("beforeend", html);

        // Increase index for next conference
        conferenceIndex++;

        updateConferenceEmptyState();
        updateConferenceNumbers();
    }


    // =========================================================
    // REMOVE CONFERENCE
    // =========================================================

    function removeConference(button) {

        const item = button.closest(".conference-item");

        if (!item) {
            return;
        }

        Swal.fire({
            icon: "warning",
            title: "Remove Conference?",
            text: "Are you sure you want to remove this conference?",
            showCancelButton: true,
            confirmButtonText: "Yes, Remove",
            cancelButtonText: "Cancel",
            confirmButtonColor: "#dc3545"
        }).then((result) => {

            if (result.isConfirmed) {

                item.remove();

                updateConferenceEmptyState();
                updateConferenceNumbers();

            }

        });
    }


    // =========================================================
    // UPDATE CONFERENCE NUMBERS
    // =========================================================

    function updateConferenceNumbers() {

        const items =
            document.querySelectorAll(".conference-item");

        items.forEach((item, index) => {

            const number =
                item.querySelector(".conference-number");

            if (number) {
                number.textContent =
                    String(index + 1).padStart(2, "0");
            }

        });
    }


    // =========================================================
    // EMPTY STATE
    // =========================================================

    function updateConferenceEmptyState() {

        const container =
            document.getElementById("conferenceContainer");

        const empty =
            document.getElementById("conferenceEmptyState");

        if (!container || !empty) {
            return;
        }

        const items =
            container.querySelectorAll(".conference-item");

        empty.style.display =
            items.length === 0 ? "block" : "none";
    }


    // =========================================================
    // INITIALIZE EXISTING RECORDS
    // =========================================================

    document.addEventListener("DOMContentLoaded", function () {

        const container =
            document.getElementById("conferenceContainer");

        if (container) {

            const items =
                container.querySelectorAll(".conference-item");

            // Continue indexing after existing records
            conferenceIndex = items.length;

        }

        updateConferenceEmptyState();
        updateConferenceNumbers();

    });

</script>